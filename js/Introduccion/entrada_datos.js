export function entrada_datos() {
    let nombre;
    let edad;
    nombre = prompt('Ingrese su nombre a continuacion: ')
    edad = prompt('Ingrese su edad a continuacion: ')
    document.write(nombre + '<br>' + edad)
}